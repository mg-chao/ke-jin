import { RegularBreakpoints } from "@mui/material";

export const FormResponsive: Record<string, RegularBreakpoints> = {
    two: {
        xs: 12,
        sm: 6,
    },
    one: {
        xs: 12,
    },
};
