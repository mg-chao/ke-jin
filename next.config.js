/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    swcMinify: true,
    swcMinify: true,
    images: {
        unoptimized: true,
    },
};

module.exports = nextConfig;
